<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CajeroMovil.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CajeroMovil.MVVM.Views"
    Shell.FlyoutBehavior="Disabled"
    >
    <!-- 
    <ShellContent
        Title="Cajero Movil"
        ContentTemplate="{DataTemplate local:itemsList}"
        Route="MainPage" />
    </Shell>
    -->
    <TabBar>
        <Tab Title="Lista">
            <ShellContent ContentTemplate="{DataTemplate local:itemsList}" />
        </Tab>
        <Tab Title="Pagar">
            <ShellContent ContentTemplate="{DataTemplate local:Pagar}" />
        </Tab>
        <Tab Title="Contacto">
            <ShellContent ContentTemplate="{DataTemplate local:Contacto}" />
        </Tab>
    </TabBar>

</Shell>